const { productRouter } = require('./product');
const { userRouter } = require('./user');
const { ratingRouter } = require('./rating')

module.exports = { productRouter, userRouter, ratingRouter};